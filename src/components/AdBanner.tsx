const AdBanner = () => {
  return (
    <div className="w-full bg-secondary/50 border border-border rounded-lg flex items-center justify-center py-3">
      <span className="text-[10px] text-muted-foreground tracking-widest uppercase">Ad Space</span>
    </div>
  );
};

export default AdBanner;
